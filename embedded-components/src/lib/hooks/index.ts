/**
 * Shared hooks used across multiple components
 *
 * These hooks are workspace-level utilities that can be used by any component.
 */

export { useDialogState } from './useDialogState';
export type { UseDialogStateReturn } from './useDialogState';

export { useIPAddress } from './useIPAddress';
