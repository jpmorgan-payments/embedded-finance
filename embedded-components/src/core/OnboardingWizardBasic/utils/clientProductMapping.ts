import { ClientProduct } from '@/api/generated/smbdo.schemas';

export const CLIENT_PRODUCT_MAPPING: Record<ClientProduct, string> = {
  EMBEDDED_PAYMENTS: 'Embedded Payments',
  MERCHANT_SERVICES: 'Merchant Services',
};
