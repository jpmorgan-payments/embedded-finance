# Implementation Plans

Generated by the improve skill on 2026-06-10 at commit `1663e9a3` (branch `feature/latest-component-enhancements`). **Scope: the `embedded-components` package only** (per the operator's instruction "only embedded components"); findings in `app/client-next-ts` were identified but deliberately not planned — see "Out of scope" below.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done. All verification commands run from `embedded-components/` with Yarn 4; on Windows PowerShell chain commands with `;`, never `&&`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](001-consolidate-toast-to-sonner.md) | Route NotificationService through sonner; delete dead legacy toast stack + `@radix-ui/react-toast` | P1 | S | — | TODO |
| [002](002-fix-unprefixed-tailwind-classes.md) | Fix malformed/unprefixed `eb-` Tailwind classes (stepper, input, sonner theming); tighten eslint guard | P1 | S | 001 (soft) | TODO |
| [003](003-stepper-keyboard-accessibility.md) | Make clickable Stepper steps keyboard-accessible (WCAG 2.1.1) | P2 | M | 002 (soft) | TODO |
| [004](004-remove-dead-dependencies.md) | Remove dead deps: `yup`, `postcss-preset-mantine`, `postcss-simple-vars` | P2 | S | — | TODO |
| [005](005-migrate-react-window-to-tanstack-virtual.md) | Migrate IndustryTypeSelect to `@tanstack/react-virtual`; drop `react-window` | P3 | M | — | TODO |
| [006](006-enforce-coverage-thresholds.md) | Coverage-threshold ratchet in Vitest config | P3 | S | 001, 003 (soft) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 after 001** (soft): 001 deletes `toast.tsx`, which contains one of the malformed classes (`sm:eb-eb-bottom-0`); running 001 first satisfies 002's `eb-eb-` done-criterion for free. 002 handles the other order too (see its Scope note).
- **003 after 002** (soft): both edit `stepper.tsx`; sequential execution avoids merge friction. No semantic dependency.
- **006 last** (soft): 001 and 003 add/remove tests, moving the coverage baseline; measuring the ratchet after they land avoids re-measuring.
- 004 and 005 are independent of everything and of each other.

## Backlog — found, vetted, not planned (ask to expand into plans)

- **react-day-picker v8 → v9 migration**: single usage site (`src/components/ui/calendar.tsx`); v8 is in maintenance. S-M effort. Low urgency.
- **UI primitive test expansion**: 36 of ~48 `src/components/ui/*.tsx` have no tests; highest-risk untested customs are `phone-input.tsx` and `calendar.tsx` (plan 003 covers `stepper.tsx`). Existing `toast`/`tooltip` tests are render-only. Pairs with raising the plan-006 ratchet.
- **Storybook stories + a11y gate**: only `dropzone.story.tsx` exists for ui primitives; `@storybook/addon-a11y` is installed but not used as a gate.
- **Investigate `eslint-config-airbnb` / `eslint-config-airbnb-typescript`**: in devDeps but not extended by `embedded-components/.eslintrc.cjs`; the monorepo root `.eslintrc.js` may use them. Verify before removing.
- **`resolutions` block pruning** in `embedded-components/package.json` after dep removals (004/005) land.
- **Version alignment with consumer app** (lucide-react 0.418 vs 0.476, tailwind-merge v2 vs v3): real but consumer-side; revisit if a styling discrepancy is reported.

## Out of scope this run (app/client-next-ts findings, vetted but unplanned per operator scope)

Recorded so the next audit doesn't redo the work:

- App `Button` declares `asChild` but ignores it (`app/client-next-ts/src/components/ui/button.tsx:44-45` — `const Comp = 'button'`).
- App ships a fully hand-rolled non-Radix `Select` (`app/client-next-ts/src/components/ui/select.tsx`) — a11y risk vs the Radix-based library version.
- SellSense theme colors hardcoded in **three** places (`tailwind.config.js:147-156`, `use-sellsense-themes.ts:335-349`, and 10+ arbitrary-value classes in `theme-utils.ts`).
- `components.json` has empty `tailwind.config` path; `dialog.tsx:24` uses `bg-white/70` overlay (drifts from library).

## Findings considered and rejected

- **`cn()`/tailwind-merge misconfigured for the `eb-` prefix**: rejected — `src/lib/utils.ts:7-28` correctly uses `extendTailwindMerge({ prefix: 'eb-' })` with custom class groups.
- **`useToast` listener "memory leak" (`use-toast.tsx:181` `[state]` dep)**: stock shadcn pattern with working cleanup; moot — plan 001 deletes the file.
- **"UI primitives carry semver weight as public API"**: rejected — `src/index.tsx` exports only widgets; `src/components/ui/index.ts` is an internal barrel, primitives are not public API.
- **`eslint-config-mantine` unused**: rejected — `.eslintrc.cjs:3` actively extends `'mantine'`; removing it would change lint behavior.
- **`uuid ^14` "suspicious version"**: rejected — valid current version, used in 4 places.
- **Badge focus classes / missing `data-slot` attrs / lucide version pin (app)**: not worth doing — cosmetic, no observed cost.
- **Input focus styles "broken" by `focus-visible:eb-default`**: downgraded — the token is junk but the real focus ring classes are present and work; cleanup folded into plan 002.
