version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - cd app
        - yarn install
    build:
      commands:
        - cd client
        - yarn install
        - yarn run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /app/client/dist
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
