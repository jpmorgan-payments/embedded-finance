version: 1
applications:
  - appRoot: app/client
    frontend:
      phases:
        build:
          commands:
            - ls
            - yarn install
            - yarn run build
      artifacts:
        # IMPORTANT - Please verify your build output directory
        baseDirectory: /app/client/dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
  - appRoot: app/client-next-ts
    frontend:
      phases:
        build:
          commands:
            - ls
            - npm install
            - npm run build
      artifacts:
        # IMPORTANT - Please verify your build output directory
        baseDirectory: /app/client-next-ts/dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*